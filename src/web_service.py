# offer up an API giving access to CE reports and badgefile contents
#   GET /reports
#   GET /reports/:report
#   GET /badgefile.csv
#   GET /badgefile.json
#   POST /attendees/:attendee
#   POST /reglist/:reglistid/attendee

# also have static content for congress directory and badge artifacts, authenticated via...
#   - magic link e-mailed to usgo.org or gocongress.org e-mail address
#   - shared magic link, QR code pasted in staff room

